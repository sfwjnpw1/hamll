server:
  port: 8084
feign:
  okhttp:
    enabled: true # 开启OKHttp连接池支持
  sentinel:
    enabled: true # 开启Feign对Sentinel的整合
hm:
  swagger:
    title: 交易服务接口文档
    package: com.hmall.trade.controller
  db:
    database: hm-trade
spring:
  rabbitmq:
    host: localhost  # mqIP
    port: 5672 # 端口，非15672
    virtual-host: /hmall # 虚拟主机
    username: hmall      # mq用户名
    password: 030826Wjy@ # mq密码